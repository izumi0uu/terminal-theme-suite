interface:
  display_name: "Derive Theme from Image"
  short_description: "Derive readable, target-aware themes from reference images"
  default_prompt: "Use $derive-theme-from-image to derive and validate a target-aware semantic theme from this reference image."
